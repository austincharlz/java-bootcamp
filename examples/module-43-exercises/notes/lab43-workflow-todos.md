# Lab 43 — Fill ci.yml TODOs

## Step 1 — Skeleton

Create notes `ci.yml.skeleton` with blanks for `java-version`, verify command, artifact paths, and package `if:`
condition.

## Step 2 — Fill

Fill blanks using Temurin 21 and `mvn -B clean verify` / package-once pattern. Note the live file is
`java-bootcamp/.github/workflows/crm-ci.yml`.

## Step 3 — Secrets comment

Add a YAML comment: `# secrets via GitHub Actions secrets — never hardcode`.

## Step 4 — Scope

State that pushing and greening the workflow is Lab 43, not this exercise.

## Scope

Pre-lab only — do not finish the full graded lab in this exercise.