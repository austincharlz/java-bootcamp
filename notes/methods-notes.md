Pass Criteria:
1. Pass
2. Pass

Pre-Lab Questions:
1. Stack frames? When main calls Calculator.add, the JVM pushes a new stack frame onto the stack. Once returned, the frame is popped from the stack. 