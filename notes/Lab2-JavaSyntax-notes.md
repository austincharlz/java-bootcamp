Notes:
*   All three .java files are under the "com.academy.student" package. 
    A single scanner object is used to get user input and used conventions to auto consume the parsing bug.
    The students are held in a fixed-sized array while studentCount keeps track of the students added. 