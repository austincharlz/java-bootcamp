# Lab 37 — Fill DDL TODOs

## Step 1 — Paste

Create `notes/lab37-todos.sql`:

```sql
CREATE TABLE customer (
  customer_id   VARCHAR(32) PRIMARY KEY,
  full_name     VARCHAR(200) NOT NULL,
  status        VARCHAR(16) NOT NULL,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT customer_status_chk CHECK (status IN ('ACTIVE','SUSPENDED'))
);

CREATE TABLE account (
  account_id     BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  customer_id    VARCHAR(32) NOT NULL REFERENCES customer(customer_id),
  account_number VARCHAR(32) NOT NULL UNIQUE,
  account_type   VARCHAR(16) NOT NULL
);

-- TODO seed
INSERT INTO customer (customer_id, full_name, status) VALUES
  ('CUS-1001', 'Amina Khan', 'ACTIVE'),
  ('CUS-1002', 'Ravi Singh', 'ACTIVE');
```