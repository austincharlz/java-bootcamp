# Lab 44 — Plan Staging Smoke

## Step 1 — Cases

Readiness + **`GET /api/customers?status=ACTIVE`** (Amina appears in the list). Optional second call without filter for
Ravi `PROSPECT`. Correlation header. On k3d use Host header `:8088` — not `GET /api/customers/{id}`.

## Step 2 — Evidence

List screenshot names under `notes/screenshots/lab-44/`.

## Step 3 — Forbidden

No production data, no real emails, no secret URLs in evidence.

## Step 4 — Scope

Plan only—execution is Lab 44.

## Scope

Pre-lab only — do not finish the full graded lab in this exercise.