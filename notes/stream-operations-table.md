#### Stream Operations Table
| Operation / API                  | Used? | Where (method / menu)                                                                                                                                                                      | Notes                                                                                                                           |
| -------------------------------- | :---: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| Lambda `forEach`                 |   ✅   | `displayAllEmployees()`, `demonstrateLambdas()`, `displayGroupedEmployees()`, many others                                                                                                  | Examples: `employee -> System.out.println(employee.getName())`, `e -> System.out.println("  " + e.getName())`                   |
| `Predicate`                      |   ✅   | `demonstrateFunctionalInterfaces()`                                                                                                                                                        | `Predicate<Employee> highEarner = employee -> employee.getSalary() > 100_000;`                                                  |
| `Function`                       |   ✅   | `demonstrateFunctionalInterfaces()`                                                                                                                                                        | `Function<Employee, String> employeeSummary = employee -> employee.getName() + " (" + employee.getDepartment() + ")";`          |
| `Consumer`                       |   ✅   | `demonstrateFunctionalInterfaces()`                                                                                                                                                        | `Consumer<Employee> printRating = employee -> System.out.println(employee.getName() + " - Rating " + employee.getRating());`    |
| `Supplier`                       |   ✅   | `demonstrateFunctionalInterfaces()`                                                                                                                                                        | Supplies the highest-paid employee using `employees.stream().max(...)`                                                          |
| `filter`                         |   ✅   | `displayHighSalaryEmployees()`, `displayItEmployees()`, `displayActiveEmployees()`, `displayFilteredItTopPerformers()`, `displayCounts()`, `demonstrateCollectors()`, `getTopPerformers()` | Filters employees by salary, department, rating, or active status                                                               |
| `map`                            |   ✅   | `demonstrateFunctionalInterfaces()`, `demonstrateStreamSources()`, `demonstrateMapping()`, `displayDistinctDepartments()`, `displayReductions()`, `ReportService.displayDashboard()`       | Maps employees to names, salaries, departments, etc.                                                                            |
| `sorted`                         |   ✅   | `demonstrateSorting()`, `displayDistinctDepartments()`, `displayTopAndNextSalaries()`, `getTopSalaries()`, `getTopPerformers()`                                                            | Uses `Comparator.comparing...()` and `reversed()`                                                                               |
| `distinct`                       |   ✅   | `displayDistinctDepartments()`, `ReportService.displayDashboard()`                                                                                                                         | Removes duplicate department names                                                                                              |
| `limit` / `skip`                 |   ✅   | `demonstrateFunctionalInterfaces()`, `demonstrateStreamSources()`, `demonstrateMapping()`, `demonstrateSorting()`, `displayTopAndNextSalaries()`, `getTopSalaries()`                       | Used to show only the first or next few results                                                                                 |
| `count`                          |   ✅   | `displayCounts()`, `ReportService.displayDashboard()`                                                                                                                                      | Counts IT employees, active employees, departments, etc.                                                                        |
| `reduce`                         |   ✅   | `displayReductions()`                                                                                                                                                                      | Uses `reduce(Double::max)` and `reduce(Double::min)`                                                                            |
| `collect(toList/toSet)`          |   ✅   | `demonstrateCollectors()`                                                                                                                                                                  | Uses `Collectors.toList()` and `Collectors.toSet()`                                                                             |
| `groupingBy`                     |   ✅   | `demonstrateCollectors()`, `displayGroupedEmployees()`, `getDepartmentStatistics()`, `findDepartmentWithHighestAverageSalary()`                                                            | Groups employees by department                                                                                                  |
| `partitioningBy`                 |   ✅   | `displayPartitionedEmployees()`                                                                                                                                                            | Partitions employees into salary > $100,000 and ≤ $100,000                                                                      |
| `summarizingDouble`              |   ✅   | `displaySummaryStatistics()`, `getDepartmentStatistics()`, `ReportService.displayDashboard()`                                                                                              | Produces count, sum, average, min, and max salary statistics                                                                    |
| `Optional` (`max` / `ifPresent`) |   ✅   | `displayHighestPaidEmployeeOptional()`, `findHighestPaidEmployee()`, `findTopPerformer()`, `ReportService.displayDashboard()`                                                              | Uses `max()`, `ifPresent()`, `ifPresentOrElse()`, and `orElse()`                                                                |
| Method references                |   ✅   | Throughout `EmployeeService` and `ReportService`                                                                                                                                           | Examples: `Employee::getSalary`, `Employee::getName`, `Employee::isActive`, `System.out::println`, `Double::max`, `Double::min` |
| Dashboard composed report        |   ✅   | **Menu 8** (`ReportService.displayDashboard()`)                                                                                                                                            | Combines summary statistics, counts, top performer, highest-paid department, and top 5 salaries into one report                 |
