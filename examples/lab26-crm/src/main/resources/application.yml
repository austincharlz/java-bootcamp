spring:
  application:
    name: northstar-crm
  # set a default profile comment; do not hard-code prod secrets
  profiles.default: dev
server:
  port: 8080
northstar:
  integration:
    # bind via NorthstarIntegrationProperties (base defaults only)
    api-base-url: http://localhost:9090
    connect-timeout-ms: 2000