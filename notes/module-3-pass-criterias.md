#### Exercise 1:
- Pass
- Pass
- Pass
- Pass | Main should only be used by the user to navigate the program, while Account owns the balance itself and has a main responsibility of performing withdrawals.
#### Exercise 2:
- Pass
- Pass
- Pass | No public setBalance exists because anyone could access it and manipulate it via external code. 
#### Exercise 3:
- Pass
- Pass
- Pass | Super is the term than allows a class to use methods and constructors from its parent class. Runtime polymorphism uses method overriding to allow the same method call to behave differently for different objects. 
#### Exercise 4:
- Pass
- Pass
- Pass | Abstract forces subclasses to implement because it does not have a body/implementation for the methods. Subclasses will have to implement those abstract methods. 
#### Exercise 5:
- Pass
- Pass
- Pass | Extends are used for abstract classes while implements are used for implementing interfaces.