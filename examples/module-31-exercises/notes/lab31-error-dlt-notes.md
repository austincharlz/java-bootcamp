# Lab 31 — Error and DLT Notes

## Step 1 — Retryable

Example: transient network blip calling email API — retry.

## Step 2 — Non-retryable

Example: JSON missing `customerId` — DLT after limited attempts.

## Step 3 — Ops note

Support replays DLT after fixing the consumer, using correlation `lab-request-001`.

## Step 4 — No runtime

I will not publish to DLT from CLI in this pre-lab.

## Scope
Pre-lab only — do not finish the full graded lab in this exercise.