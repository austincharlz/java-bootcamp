# Lab 46 — Fill Metrics/Alert TODOs

## Step 1 — Template

Fill:

```bash
Processed count: `customer_events_processed_total` Failed count: `customer_events_failed_total` Retry count: `customer_events_retry_total` DLT count: `customer_events_dlt_total` Lag signal: `kafka_consumer_lag` Alert if lag > `50` for `5m`
```

## Step 2 — Panels

List three dashboard panels tied to agent impact (lag, DLT growth, error rate).

## Step 3 — Cardinality

Strike any plan that tags metrics with raw email addresses.

## Step 4 — Scope

Checklist only—wiring Micrometer is Lab 46.

## Scope

Pre-lab only — do not finish the full graded lab in this exercise.